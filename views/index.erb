<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Hangman</title>
</head>
<body>

  <h1>Hangman</h1>

  <p>Guesses left: <%= guesses_left %></p>
  <p>Wrong characters: <%= wrong_characters %></p>

  <br>

  <p><%= hidden_word %></p>

  <br>

  <% unless game_finished %>
    <p>Introduce a letter or the complete word:</p>
  <% end %>

  <% if game_result == :player_wins %>
    <p>Correct!</p>
  <% elsif game_result == :player_loses %>
    <p>The word was: <%= secret_word %></p>
  <% end %>

  <% unless game_finished %>
    <form>
      <input type="text" name="guess" autofocus>
    </form>
  <% else %>
    <form>
      <input type="submit" name="restart" value="Next word!" autofocus>
    </form>
  <% end %>

</body>
</html>
